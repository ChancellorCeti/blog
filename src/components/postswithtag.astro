---
const { posts, tag } = Astro.props;
---

<h2>{tag}</h2>
{posts.map((post) => <a href={post.url}>{post.frontmatter.title}</a>)}
<style>
  h2 {
    color: white;
  }
  a {
    color: white;
  }
</style>
